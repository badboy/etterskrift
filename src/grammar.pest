program = {
  SOI ~
  (item | block)* ~
  EOI
}

char = _{ ASCII_ALPHA | "." | "_" | "-" }
number = @{ ASCII_DIGIT+ }
ident = @{ char+ }
key = ${ "/" ~ ident }
ops = { "[" | "]" }
item = { (ident | key | number | ops) }
block = { "{" ~ (item | block)* ~ "}" }

WHITESPACE = _{ " " | "\n"}
