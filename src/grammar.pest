program = {
  SOI ~
  item* ~
  EOI
}

char = _{ ASCII_ALPHA | "." | "_" | "-" }
number = @{ ASCII_DIGIT+ }
ident = @{ char+ }
key = ${ "/" ~ ident }
ops = { "[" | "]" | "{" | "}" }
item = { (ident | key | number | ops) }

WHITESPACE = _{ " " | "\n"}
